方案一：

根据前台返回的解码器包使用jdeps生成decoder包下的依赖关系文件。（或者由前台进行分析）
编写脚本分析依赖文件，并改写pom配置，使用maven-shade
exclude不需要的包。修改单元测试的配置，include需要的测试。
使用mavn命令进行打包

问题：需要写脚本分析依赖文件

方案二：

使用proguard，根据前台返回的解码器列表，修改proguard配置文件，将需要的解码器加入keep选项并启用shrink配置。
使用mvn打包

问题：shrink 并混淆后不知道能不能运行单元测试
问题：编写配置文件比较麻烦,要写很多keep选项
